apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert2jira.fullname" . }}
  labels:
    {{- include "alert2jira.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alert2jira.selectorLabels" . | nindent 4 }}
